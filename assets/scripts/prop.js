
var prop={};

function prop_init() {
    prop.about={};
    prop.about.version=[0,0,1];
    prop.network={};
    prop.network.url={};
    prop.network.url.root="";
    prop.loaded=false;
    loaded("prop");
}